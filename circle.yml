machine:
  java:
    version: oraclejdk8
dependencies:
  override:
    - sbt clean test:compile
test:
  override:
    - sbt test
deployment:
  production:
    branch: echo
    commands:
      - ./deploy.sh
